spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Test_NG
    username: adsuser
    password: AdS@3421
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080
  servlet:
    context-path: /TestNG